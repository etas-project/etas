[package]
name = "release-planner"
version = "0.1.0"
edition = "2026"
license = "MIT"
description = "Project-level release planning application fixture"
repository = "https://example.invalid/etas-fixtures/release-planner"

[source]
root = "src"

[dependencies]
std = { version = "0.1" }

[[bin]]
name = "release-planner"
module = "app.main"
flow = "main"
