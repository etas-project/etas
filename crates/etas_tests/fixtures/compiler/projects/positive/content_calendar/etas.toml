[package]
name = "content-calendar"
version = "0.1.0"
edition = "2026"
license = "MIT"
description = "Project-level content calendar application fixture"
repository = "https://example.invalid/etas-fixtures/content-calendar"

[source]
root = "src"

[dependencies]
std = { version = "0.1" }

[[bin]]
name = "content-calendar"
module = "app.main"
flow = "main"
