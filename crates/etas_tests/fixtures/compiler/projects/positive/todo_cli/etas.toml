[package]
name = "todo-cli"
version = "0.1.0"
edition = "2026"
license = "MIT"
description = "Project-level todo command-line application fixture"
repository = "https://example.invalid/etas-fixtures/todo-cli"

[source]
root = "src"

[dependencies]
std = { version = "0.1" }

[[bin]]
name = "todo-cli"
module = "app.main"
flow = "main"
