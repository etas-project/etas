[package]
name = "inventory-reorder"
version = "0.1.0"
edition = "2026"
license = "MIT"
description = "Project-level inventory reorder application fixture"
repository = "https://example.invalid/etas-fixtures/inventory-reorder"

[source]
root = "src"

[dependencies]
std = { version = "0.1" }

[[bin]]
name = "inventory-reorder"
module = "app.main"
flow = "main"
