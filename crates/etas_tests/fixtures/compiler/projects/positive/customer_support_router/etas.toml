[package]
name = "customer-support-router"
version = "0.1.0"
edition = "2026"
license = "MIT"
description = "Project-level customer support routing application fixture"
repository = "https://example.invalid/etas-fixtures/customer-support-router"

[source]
root = "src"

[dependencies]
std = { version = "0.1" }

[[bin]]
name = "customer-support-router"
module = "app.main"
flow = "main"
