# Agent System Smoke

This fixture exercises project import resolution, effect checking, an agent call, model host execution, and console output.

Manual live run:

```bash
export ETAS_HOST_OMLX_API_KEY='<your-local-omlx-key>'
etas run . --profile local-omlx --budget-tokens 128
```
