[package]
name = "incident-runbook"
version = "0.1.0"
edition = "2026"
license = "MIT"
description = "Project-level incident runbook application fixture"
repository = "https://example.invalid/etas-fixtures/incident-runbook"

[source]
root = "src"

[dependencies]
std = { version = "0.1" }

[[bin]]
name = "incident-runbook"
module = "app.main"
flow = "main"
