[package]
name = "duplicate-top-level-item"
version = "0.1.0"
edition = "2026"
license = "MIT"
description = "Negative project-level package-management fixture for duplicate_top_level_item"
repository = "https://example.invalid/etas-fixtures/duplicate-top-level-item"

[source]
root = "src"

[dependencies]
std = { version = "0.1" }

[[bin]]
name = "duplicate-top-level-item"
module = "app.main"
flow = "main"
